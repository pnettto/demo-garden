services:
  spotify-client:
    build: ./backend/
    env_file:
      - .env
    profiles:
      - lazy
    labels:
      - garbage_collect
    volumes:
      - /app/db
    expose:
      - 8000
    restart: on-failure

# Create dummy certs for nginx to start on localhost
# Add to nginx certs folder
openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout private.key \
  -out fullchain.crt \
  -subj "/C=US/ST=State/L=City/O=Organization/CN=localhost"
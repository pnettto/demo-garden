services:
  python-go-test:
    build: .
    profiles:
      - lazy
    labels:
      - "lazy=true"
    expose:
      - "8000"
    environment:
      DB_HOST: python-app-db
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
    depends_on:
      - python-app-db
      - go-worker
    restart: on-failure

  python-app-db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_HOST_AUTH_METHOD: trust
    profiles:
      - lazy
    labels:
      - "lazy=true"
    volumes:
      - python-app-postgres:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  python-app-postgres:

services:
  lazy-manager:
    build: .
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ../../:/demos-dir
    environment:
      IDLE_TIMEOUT: 10 # seconds
      PROJECT_NAME: demos
      DEMOS_DIR: /demos-dir
      PYTHONUNBUFFERED: "1"
    expose:
      - "8001"
    restart: unless-stopped
